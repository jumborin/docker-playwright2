{
  "name": "@local/playwright-excel-automation",
  "version": "1.0.0",
  "description": "Playwright UI test automation with Excel integration",
  "main": "index.js",
  "scripts": {
    "test": "cross-env LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 LESSCHARSET=utf-8 NODE_OPTIONS='--max-old-space-size=4096' playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "install-browsers": "playwright install"
  },
  "dependencies": {
    "@playwright/test": "^1.57.0",
    "xlsx": "^0.18.5",
    "node-cron": "^3.0.3",
    "ts-node": "^10.9.0",
    "cross-env": "^7.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}