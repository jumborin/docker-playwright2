{
  "name": "@local/playwright-excel-automation",
  "version": "1.0.0",
  "description": "Playwright UI test automation with Excel integration",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "install-browsers": "playwright install"
  },
  "dependencies": {
    "@playwright/test": "^1.57.0",
    "xlsx": "^0.18.5",
    "node-cron": "^3.0.3",
    "ts-node": "^10.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}